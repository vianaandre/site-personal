import { createContext } from 'react';
import ProdutosCardsProps from '../../types/produtosCardsProps';

const ContextProdutos = createContext({} as ProdutosCardsProps)

export default ContextProdutos;
