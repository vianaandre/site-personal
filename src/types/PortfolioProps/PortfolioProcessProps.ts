import { CardProcessProps, TitleProps } from "../types"

type PortfolioProcessProps = {
  sectionTitle: TitleProps,
  titleCards: Array<TitleProps>,
  CardProcess: Array<CardProcessProps>
}

export default PortfolioProcessProps
